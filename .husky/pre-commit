# Validate registry item descriptions
if git diff --cached --name-only | grep -q 'registry/.*item\.json'; then
  bash "$(git rev-parse --show-toplevel)/scripts/check-descriptions.sh"
fi

# Design system lint on staged TS/TSX files
npx lint-staged
